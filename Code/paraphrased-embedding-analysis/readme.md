# Paraphrased Embedding Analysis

This project is used as a tool for the preliminary study of the vector spaces created by embedding software requirements.
It analyzes the distribution of paraphrased requirements in vector space and their similarity between different requirements.
To execute the analysis, configure it in the config.json and execute the main Method.